import { render, screen } from '@testing-library/react';
import App from './App';

test.todo('renders parent component', /* () => {
  // render(<App />);
  // const linkElement = screen.getByText('Parent Component');
  // expect(linkElement).toBeInTheDocument();
} */);

/* test('sum of const should be 10',()=>{
  render(<App />);
  const sum = screen.getByTestId('sum');
  expect(sum.textContent).toBe("10");
})

test('render city name in list',()=>{
  render(<App />);
  const listItem = screen.getAllByTitle('listItem');
  expect(listItem.length).toBe(1);
}) */
